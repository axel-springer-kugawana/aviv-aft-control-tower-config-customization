AWSTemplateFormatVersion: "2010-09-09"
Description: "AWS CloudFormation Template to update config recorder settings in child accounts created by ControlTower."
Parameters:
  CloudFormationVersion:
    Description: Update Version every time stack is updated to force rerun of the solution.
    Type: String
    Default: 1

  AccountSelectionMode:
    Description: Account selection mode - EXCLUSION (processes all accounts except those in ExcludedAccounts) or INCLUSION (processes only accounts in IncludedAccounts). Default is EXCLUSION for backward compatibility.
    Type: String
    Default: INCLUSION
    AllowedValues:
      - EXCLUSION
      - INCLUSION

  ExcludedAccounts:
    Description: Excluded Accounts list. This list should contain Management account, Log Archive and Audit accounts at the minimum and other accounts you want to be excluded. Only used when AccountSelectionMode is EXCLUSION.
    Type: String
    Default: "['111111111111', '222222222222', '333333333333']"
    MinLength: 36
    MaxLength: 4096

  IncludedAccounts:
    Description: List of AWS account IDs to include when AccountSelectionMode is INCLUSION. Only used when AccountSelectionMode is INCLUSION.
    Type: String
    Default: "['498757326706', '417589271012', '722853356539', '895761800152', '480494037506', '055806808485', '952085476791', '823281049777', '014498631914', '300903263990', '645773005065', '287167054505', '924132661797', '654654617653', '694368034997', '730335339132', '752735322755', '005097884673', '561876735519', '428156589096', '044878483794', '975050261781', '308438406637', '381491985139', '528757812796', '465028495829', '637423199322', '014498632098', '090290096726', '731451715286', '135557783010', '038462748721', '301457867660', '699866662249', '816069170111', '617541205870', '476572384358', '841641724002', '477276785702', '695418593809', '719623059185', '301691474736', '612923061438', '825765405967', '656370814068', '800185712387', '012262792669', '590184066082', '971444785718', '972611417827', '590183964085', '289896345525', '042776340025', '445567107432', '775161746977', '292714939474', '577638365894', '220509570456', '334346482537', '844163900855', '194525820442', '944082872160', '225187408232', '274743428196', '638515252345', '640168449524', '619885063415', '855734488874', '211125520952', '420435663485', '575108932836', '785779968547', '339712942584', '535182441685', '048248989915', '756912721607', '603083255474', '418295707376', '989580981396', '238791458351', '962639302875', '589383485585', '618026166328', '630579746893', '951707862660', '354307071246', '093518501487', '890742610529', '667186907426', '272575627684', '211125551580', '490872067923', '502976064466', '406990755989', '816361907671', '188847977040', '590183822658', '920030175053', '699357209362', '504410438058', '590183885706', '965128646624', '767398124311', '817046735440', '050027657009', '314779777683', '148622481066', '560858388449', '187478111767', '481385706671', '535237074269', '211125588068', '980921756015', '021859651675', '518951787406', '877244303612', '995501883038', '065094257726', '929426909506', '510849591313', '891377333918', '224806504407', '406933106193', '445590720233', '211023521443', '392226992659', '252457242436', '520097583112', '634319551885', '637423492670', '693751107654', '891377398678', '253859328753', '202533527687', '495762834248', '247726796311', '375740440665', '533266971566', '631721122707', '107346958065', '738982231655', '582093722273', '172026079226', '146072879880', '339713071724', '053031736231', '811734573808', '118156525845', '767828747397', '651767347118', '957710774205', '507532294300', '292725948070', '349769753824', '913418658603', '264778640073', '905418058602', '443204902571', '024848444655', '907502446382', '111315604544', '767397656124', '008583020256', '008971657600', '458945172370', '112443477392', '799314659464', '606876783784', '961156486339', '851725188848', '058264322962', '897722661710', '891377034316', '767397846114', '388449571037', '365827924862', '339712945754', '061039786117', '535002848499', '554051924802', '790724769062', '154541629937', '211125467014', '574630139193', '911558392221', '167041753247', '879381252354', '343218177920', '654654309225', '016365604331', '823963318160']"
    MinLength: 2
    MaxLength: 8192

  ConfigRecorderStrategy:
    Default: INCLUSION
    Description: Config Recorder Strategy. INCLUSION records only ConfigRecorderIncludedResourceTypes. Unrelated to AccountSelectionMode, which selects accounts.
    Type: String
    AllowedValues:
      - EXCLUSION
      - INCLUSION

  ConfigRecorderExcludedResourceTypes:
    Description: List of all resource types to be excluded from Config Recorder if you pick EXCLUSION strategy
    Default: "AWS::HealthLake::FHIRDatastore,AWS::Pinpoint::Segment,AWS::Pinpoint::ApplicationSettings"
    Type: String

  ConfigRecorderIncludedResourceTypes:
    Description: Regional resource types to record when ConfigRecorderStrategy is INCLUSION. Global types belong in ConfigRecorderDailyGlobalResourceTypes so they are only recorded in the home region, avoiding duplicate configuration items across regions.
    Default: "AWS::S3::Bucket,AWS::EC2::Instance,AWS::Lambda::Function,AWS::SQS::Queue,AWS::ApiGateway::RestApi,AWS::RDS::DBInstance,AWS::EC2::EIP,AWS::EC2::EIPAssociation,AWS::RDS::DBCluster,AWS::SecretsManager::Secret,AWS::DynamoDB::Table,AWS::ACM::Certificate,AWS::ElasticLoadBalancingV2::LoadBalancer,AWS::SNS::Topic,AWS::OpenSearch::Domain,AWS::KMS::Key,AWS::ECS::Cluster,AWS::WAFv2::IPSet,AWS::WAFv2::WebACL,AWS::CodeDeploy::Application,AWS::SageMaker::Model,AWS::ApiGatewayV2::Stage,AWS::ApiGatewayV2::Api,AWS::Kinesis::Stream,AWS::EC2::VPNConnection,AWS::EC2::NetworkAcl,AWS::RDS::DBSubnetGroup,AWS::EFS::FileSystem,AWS::GuardDuty::Detector,AWS::EC2::FlowLog"
    Type: String

  ConfigRecorderDailyResourceTypes:
    Description: Resource types recorded on a daily cadence instead of continuously. In INCLUSION strategy these are automatically added to the recorded list, so only list types you actually want recorded.
    Default: "AWS::AutoScaling::AutoScalingGroup"
    Type: String

  ConfigRecorderDailyGlobalResourceTypes:
    Description: Global resource types, recorded on a daily cadence and only in the AWS Control Tower home region. In INCLUSION strategy these are automatically added to the recorded list, so only list types you actually want recorded.
    Default: "AWS::IAM::Policy,AWS::IAM::User,AWS::IAM::Group,AWS::IAM::OIDCProvider,AWS::IAM::SAMLProvider,AWS::Route53::HostedZone,AWS::CloudFront::Distribution,AWS::WAF::WebACL"
    Type: String

  ConfigRecorderDefaultRecordingFrequency:
    Description: Default Frequency of recording configuration changes.
    Default: CONTINUOUS
    Type: String
    AllowedValues:
      - CONTINUOUS
      - DAILY

  SourceS3Bucket:
    Type: String
    Default: aviv-aft-control-tower-config-customization
    Description: S3 bucket containing the Lambda deployment packages, leave it default unless you have customized code.

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: "Mandatory Settings"
        Parameters:
          - CloudFormationVersion
          - SourceS3Bucket
      - Label:
          default: "Account Selection Settings"
        Parameters:
          - AccountSelectionMode
          - ExcludedAccounts
          - IncludedAccounts
      - Label:
          default: "Recording Strategy Settings"
        Parameters:
          - ConfigRecorderStrategy
          - ConfigRecorderExcludedResourceTypes
          - ConfigRecorderIncludedResourceTypes
      - Label:
          default: " Recording Frequency Settings"
        Parameters:
          - ConfigRecorderDefaultRecordingFrequency
          - ConfigRecorderDailyResourceTypes
          - ConfigRecorderDailyGlobalResourceTypes

Resources:
  LambdaZipsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

  LambdaZipsBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket:
        Ref: LambdaZipsBucket
      PolicyDocument:
        Statement:
          - Effect: Deny
            Action: "s3:*"
            Principal: "*"
            Resource:
              - !Sub "arn:aws:s3:::${LambdaZipsBucket}"
              - !Sub "arn:aws:s3:::${LambdaZipsBucket}/*"
            Condition:
              Bool:
                aws:SecureTransport: false

  ProducerLambda:
    Type: AWS::Lambda::Function
    DeletionPolicy: Retain
    DependsOn: CopyZips
    Properties:
      Code:
        S3Bucket: !Ref LambdaZipsBucket
        S3Key: ct-blogs-content/ct_configrecorder_override_producer.zip
      Handler: ct_configrecorder_override_producer.lambda_handler
      Role: !GetAtt ProducerLambdaExecutionRole.Arn
      Runtime: python3.12
      MemorySize: 128
      Timeout: 300
      Architectures:
        - x86_64
      ReservedConcurrentExecutions: 1
      Environment:
        Variables:
          ACCOUNT_SELECTION_MODE: !Ref AccountSelectionMode
          EXCLUDED_ACCOUNTS: !Ref ExcludedAccounts
          INCLUDED_ACCOUNTS: !Ref IncludedAccounts
          LOG_LEVEL: INFO
          SQS_URL: !Ref SQSConfigRecorder

  ProducerLambdaPermissions:
    Type: AWS::Lambda::Permission
    DeletionPolicy: Retain
    Properties:
      Action: "lambda:InvokeFunction"
      FunctionName: !Ref ProducerLambda
      Principal: "events.amazonaws.com"
      SourceArn: !GetAtt ProducerEventTrigger.Arn

  ConsumerLambda:
    Type: AWS::Lambda::Function
    DeletionPolicy: Retain
    DependsOn: CopyZips
    Properties:
      Code:
        S3Bucket: !Ref LambdaZipsBucket
        S3Key: ct-blogs-content/ct_configrecorder_override_consumer.zip
      Handler: ct_configrecorder_override_consumer.lambda_handler
      Role: !GetAtt ConsumerLambdaExecutionRole.Arn
      Runtime: python3.12
      MemorySize: 128
      Timeout: 180
      Architectures:
        - x86_64
      ReservedConcurrentExecutions: 10
      Environment:
        Variables:
          LOG_LEVEL: INFO
          CONFIG_RECORDER_STRATEGY: !Ref ConfigRecorderStrategy
          CONFIG_RECORDER_OVERRIDE_DAILY_RESOURCE_LIST: !Ref ConfigRecorderDailyResourceTypes
          CONFIG_RECORDER_OVERRIDE_DAILY_GLOBAL_RESOURCE_LIST: !Ref ConfigRecorderDailyGlobalResourceTypes
          CONFIG_RECORDER_OVERRIDE_EXCLUDED_RESOURCE_LIST: !Ref ConfigRecorderExcludedResourceTypes
          CONFIG_RECORDER_OVERRIDE_INCLUDED_RESOURCE_LIST: !Ref ConfigRecorderIncludedResourceTypes
          CONFIG_RECORDER_DEFAULT_RECORDING_FREQUENCY: !Ref ConfigRecorderDefaultRecordingFrequency
          CONTROL_TOWER_HOME_REGION: !Ref "AWS::Region"

  ConsumerLambdaEventSourceMapping:
    Type: AWS::Lambda::EventSourceMapping
    DeletionPolicy: Retain
    Properties:
      BatchSize: 1
      Enabled: true
      EventSourceArn: !GetAtt SQSConfigRecorder.Arn
      FunctionName: !GetAtt ConsumerLambda.Arn

  ProducerLambdaExecutionRole:
    Type: "AWS::IAM::Role"
    DeletionPolicy: Retain
    Properties:
      ManagedPolicyArns:
        - !Sub "arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - "sts:AssumeRole"
      Path: /
      Policies:
        - PolicyName: ct_cro_producer
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - cloudformation:ListStackInstances
                Resource: !Sub "arn:${AWS::Partition}:cloudformation:*:*:stackset/AWSControlTowerBP-BASELINE-CONFIG:*"
              - Effect: Allow
                Action:
                  - sqs:DeleteMessage
                  - sqs:ReceiveMessage
                  - sqs:SendMessage
                  - sqs:GetQueueAttributes
                Resource: !GetAtt SQSConfigRecorder.Arn

  ConsumerLambdaExecutionRole:
    Type: "AWS::IAM::Role"
    DeletionPolicy: Retain
    Properties:
      ManagedPolicyArns:
        - !Sub "arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - "sts:AssumeRole"
      Path: /
      Policies:
        - PolicyName: policy-sts-all
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - sts:AssumeRole
                Resource: "*"
              - Effect: Allow
                Action:
                  - sqs:DeleteMessage
                  - sqs:ReceiveMessage
                  - sqs:SendMessage
                  - sqs:GetQueueAttributes
                Resource: !GetAtt SQSConfigRecorder.Arn

  SQSConfigRecorder:
    Type: AWS::SQS::Queue
    DeletionPolicy: Retain
    Properties:
      VisibilityTimeout: 180
      DelaySeconds: 5
      KmsMasterKeyId: alias/aws/sqs

  ProducerEventTrigger:
    Type: AWS::Events::Rule
    Properties:
      Description: "Rule to trigger config recorder override producer lambda"
      EventBusName: default
      EventPattern: '{
        "source": ["aws.controltower"],
        "detail-type": ["AWS Service Event via CloudTrail"],
        "detail": {
        "eventName": ["UpdateLandingZone", "CreateManagedAccount", "UpdateManagedAccount", "ResetLandingZone"]
        }
        }'
      Name: !GetAtt SQSConfigRecorder.QueueName
      State: ENABLED
      Targets:
        - Arn:
            Fn::GetAtt:
              - "ProducerLambda"
              - "Arn"
          Id: "ProducerTarget"

  ProducerLambdaTrigger:
    Type: "Custom::ExecuteLambda"
    Properties:
      ServiceToken: !GetAtt "ProducerLambda.Arn"
      FunctionName: !Ref ProducerLambda
      Version: !Ref CloudFormationVersion

  CopyZips:
    Type: Custom::CopyZips
    Properties:
      ServiceToken: !GetAtt "CopyZipsFunction.Arn"
      DestBucket: !Ref "LambdaZipsBucket"
      SourceBucket: !Ref SourceS3Bucket
      Prefix: ct-blogs-content/
      Objects:
        - "ct_configrecorder_override_producer.zip"
        - "ct_configrecorder_override_consumer.zip"

  CopyZipsRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Sub "arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
      Path: /
      Policies:
        - PolicyName: lambda-copier
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectTagging
                Resource: !Sub "arn:${AWS::Partition}:s3:::${SourceS3Bucket}/ct-blogs-content/*"
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:DeleteObject
                  - s3:PutObjectTagging
                Resource:
                  - !Sub "arn:${AWS::Partition}:s3:::${LambdaZipsBucket}/ct-blogs-content/*"

  CopyZipsFunction:
    Type: AWS::Lambda::Function
    Properties:
      Description: Copies objects from the S3 bucket to a new location.
      Handler: index.handler
      Runtime: python3.12
      Role: !GetAtt "CopyZipsRole.Arn"
      ReservedConcurrentExecutions: 1
      Timeout: 300
      Code:
        ZipFile: |
          import json
          import logging
          import threading
          import boto3
          import cfnresponse
          def copy_objects(source_bucket, dest_bucket, prefix, objects):
              s3 = boto3.client('s3')
              for o in objects:
                  key = prefix + o
                  copy_source = {
                      'Bucket': source_bucket,
                      'Key': key
                  }
                  print('copy_source: %s' % copy_source)
                  print('dest_bucket = %s'%dest_bucket)
                  print('key = %s' %key)
                  s3.copy_object(CopySource=copy_source, Bucket=dest_bucket,
                        Key=key)
          def delete_objects(bucket, prefix, objects):
              s3 = boto3.client('s3')
              objects = {'Objects': [{'Key': prefix + o} for o in objects]}
              s3.delete_objects(Bucket=bucket, Delete=objects)
          def timeout(event, context):
              logging.error('Execution is about to time out, sending failure response to CloudFormation')
              cfnresponse.send(event, context, cfnresponse.FAILED, {}, None)
          def handler(event, context):
              # make sure we send a failure to CloudFormation if the function
              # is going to timeout
              timer = threading.Timer((context.get_remaining_time_in_millis()
                        / 1000.00) - 0.5, timeout, args=[event, context])
              timer.start()
              print('Received event: %s' % json.dumps(event))
              status = cfnresponse.SUCCESS
              try:
                  source_bucket = event['ResourceProperties']['SourceBucket']
                  dest_bucket = event['ResourceProperties']['DestBucket']
                  prefix = event['ResourceProperties']['Prefix']
                  objects = event['ResourceProperties']['Objects']
                  if event['RequestType'] == 'Delete':
                      delete_objects(dest_bucket, prefix, objects)
                  else:
                      copy_objects(source_bucket, dest_bucket, prefix, objects)
              except Exception as e:
                  logging.error('Exception: %s' % e, exc_info=True)
                  status = cfnresponse.FAILED
              finally:
                  timer.cancel()
                  cfnresponse.send(event, context, status, {}, None)
